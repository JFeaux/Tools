#ifndef INDICES_H // header guards 
#define INDICES_H 

#include <vector> 

int c2(int i, int j, int n); 
int c3(int i, int j, int k, int n); 
int c4(int i, int j, int k, int l, int n); 
int cinf(std::vector<int> index, int n);
 
#endif
